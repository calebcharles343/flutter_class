// lib/main.dart
import 'package:flutter/material.dart';
import 'package:flutter_class1/app/app.dart';

/// 🎓 LEARNING: main() is the entry point of every Flutter app
/// This is where everything starts
void main() {
  // 🎓 LEARNING: Run the app
  // FlutterClass() is the root widget of your app
  runApp(FlutterClass());

  print("🚀 Flutter app started successfully!");
}
